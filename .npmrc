# IFLA Standards Platform - npm Configuration
# NPM-compatible settings only (pnpm-specific settings moved to .pnpmrc)

# Network Settings (npm-compatible)
fetch-retries=3
fetch-retry-factor=2
fetch-retry-mintimeout=2000
fetch-retry-maxtimeout=30000

# Security
audit-level=moderate